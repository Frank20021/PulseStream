[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: tests that do not need Docker
    integration: tests that use the running PulseStream stack
    failure: failure-injection tests that stop or restart containers
